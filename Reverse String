class Solution {
    public String reverseString(String s) {
        if(s==null){
            return null;
        }
        String res="";
        StringBuilder sb=new StringBuilder(s);
        sb.reverse();
        res=sb.toString();
        return res;
    }
}
