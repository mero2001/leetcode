class Solution {
    public int missingNumber(int[] nums) {
        int sum=0;
        int len=nums.length;
        sum=(len+1)*len/2;
        for(int i=0;i<len;i++){
            sum-=nums[i];
        }
        
        return sum;
        
    }
}
